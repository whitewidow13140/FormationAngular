import { BlockBorderDirective } from './block-border.directive';

describe('BlockBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockBorderDirective();
    expect(directive).toBeTruthy();
  });
});
